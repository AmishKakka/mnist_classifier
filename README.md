# mnist_classifier
Classifies digits with test accuracy of 94%


Here neural networks are used and the hidden layers comprise only of Dense layers.
The dataset has in total 70,000 images. No. of test images is 10,500 and rest are divided as training and validation images in 1:5 ratio.
